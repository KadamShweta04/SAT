[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
sympy = "*"
pyinstaller = "*"

[packages]
flask = "==1.*"
flask-restplus = "==0.*"
atomicfile = "==1.*"
waitress = "==1.*"
numpy ="==1.*"
flask-cors ="==3.*"
lxml = "==4.*"
networkx = "*"
pyhull = "*"
matplotlib = "*"
# this is needed to serialize named touples as dicts. Flask uses this automatically if it is present
simplejson = "*"
#to force pyinstall to include this lib
jsonschema = "==2.*"

[requires]
python_version = "3.7"
